set(TARGET main)
include_directories(${CMAKE_CURRENT_SOURCE_DIR})
add_executable(${TARGET} main.cpp)
target_link_libraries(${TARGET} PRIVATE media-reader h264 de265)
target_compile_features(${TARGET} PUBLIC cxx_std_11)